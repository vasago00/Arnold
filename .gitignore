# Dependencies
node_modules/
arnold-app/node_modules/

# Environment files — never commit credentials
.env
.env.*
arnold-garmin-api/.env
arnold-app/.env

# Build output
arnold-app/dist/

# Health data — stays local
arnold-memory/
Garmin.env.txt
*.env.txt
# Personal health-data CSV imports — NEVER commit (weight/HRV/sleep/nutrition).
# Untracked 2026-05-27; the app reads from public/data-imports/ (also
# gitignored) and live Garmin sync, never from this root copy.
data-imports/

# Python runtime
__pycache__/
*.pyc
session.json

# OS files
.DS_Store
Thumbs.db
*.log
