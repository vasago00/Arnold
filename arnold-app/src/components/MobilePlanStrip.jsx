// Superseded by MobilePlanTicker (Task #40). The tall summary-card strip was
// replaced by the slim effort-silhouette ticker on Start. Kept as a forwarding
// re-export so any lingering import resolves to the ticker.
export { default } from './MobilePlanTicker.jsx';
