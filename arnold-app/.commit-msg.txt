Phase 4r.utc.2 + maps + calendar polish

UTC sweep (task #76):
- Consolidated 15 duplicate localDate() definitions into core/time.js
- Replaced 36 bare new Date(string) patterns with parseLocalDate from core/dateUtils.js
- All date parsing now uses local-time anchors, eliminating UTC-rollover bugs

Calendar mobile (Phase 4r.calendar.20-25):
- Mobile variant of CalendarTab with compact tiles + inline always-open drawer
- 7-col Mon-Sun grid via flexbox (resolved 4-col collapse via min-width:0)
- Real session-signature PNGs at tile size with SIG_SCALE correction
- Swipe between months on the grid; tab-level swipe between Fuel/Calendar/Core
- Header chips removed, contextual race-add pills in drawer
- Past-date drawers hide add-race buttons
- Square tiles, full month visible above drawer

Nav (Phase 4r.nav.1-2 + 4r.calendar.24):
- Calendar promoted to primary bottom nav (replacing Labs)
- Labs moved to More overflow
- Calendar slot positioned between Fuel and Core
- Fixed stale Arnold.jsx SWIPE_ORDER that was crashing on swipe forward
- Web Body tab renamed to Core for cross-platform consistency

Color (Phase 4r.color.1):
- HIIT split to coral-pink #fb7185 across CalendarTab/PlannedWorkoutTile/planner
- Tempo keeps amber so the two effort classes are visually distinct at small size

Race catalog (Phase 4r.calendar.16-17):
- Race-picker month filter tightened to exact match (was +/-1 month tolerance)
- Added NYRR Achilles Hope and Possibility 4M (June) and NYRR 9/11 Memorial 4M (July)
- DayDrawer now surfaces race city and country

Cleanup (Phase 4r.cleanup.1):
- Removed unused MobileDaySheet component after switch to inline drawer
- Verified all family color, signature path, and nav state maps consistent

Maps and docs (Phase 4r.maps.1):
- Created /docs folder: ARCHITECTURE.md, DATA_FLOW.md, NAV_MAP.md, FAMILY_MAP.md, STORAGE_MAP.md, README.md
- Fixed one drift: PlannedWorkoutTile.run was pointing at non-existent run.png; aligned to easy-run.png

Build stamp: Phase 4r.utc.2
