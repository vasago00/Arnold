{
  "name": "arnold-sync-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail",
    "kv:create": "wrangler kv namespace create arnold-sync",
    "secret:token": "wrangler secret put SYNC_TOKEN"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
